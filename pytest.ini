[pytest]
# `pythonpath` lets the tests import `main`, `paths` and `services.*` the same way
# uvicorn does when launched with --app-dir backend.
pythonpath = backend
testpaths = backend/tests
addopts = -q --tb=short
filterwarnings =
    ignore::DeprecationWarning
