# DeepTrace backend runtime dependencies
# Python 3.11 on Windows. CPU PyTorch wheels are used for reproducibility.
--extra-index-url https://download.pytorch.org/whl/cpu

fastapi==0.141.1
uvicorn[standard]==0.52.4
SQLAlchemy==2.0.52
python-multipart==0.0.32

numpy==1.26.4
Pillow==10.2.0
opencv-python-headless==4.10.0.84
ImageHash==4.3.2
reportlab==5.0.1
scipy==1.17.1
soundfile==0.14.0

torch==2.2.2+cpu
torchvision==0.17.2+cpu
torchaudio==2.2.2+cpu
facenet-pytorch==2.6.0
transformers==4.41.2
SpeechBrain==1.0.3
c2pa-python==0.37.5

# Reverse-image provenance search: HTTP calls to the search API and to candidate
# pages, and HTML parsing of those pages to find the media they serve.
requests==2.34.2
beautifulsoup4==4.15.0

# Optional, not installed here: yt-dlp on PATH lets the same module read metadata
# from platforms that render client-side (Instagram, Facebook, X, YouTube). When
# it is absent those sources are reported as unverified rather than as cleared.

# Verification tools. Not imported by the running service — they are used by
# backend/tests and scripts/smoke_e2e.py, and are listed here rather than pinned
# above so a deployment install does not pull them in:
#   pip install pytest pypdf
# Without pypdf the smoke test still runs; it skips the report-content assertions
# and says so, rather than passing them silently.
